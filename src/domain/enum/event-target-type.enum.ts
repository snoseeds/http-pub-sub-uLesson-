/*These enums are expected to be valid table names that
  have relationships with subscribers or the subscriber
  table itself
*/
export enum EventTargetType {
  TOPIC = 'Topic',
  SUBSCRIBER = 'Subscriber'
}